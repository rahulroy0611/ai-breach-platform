services:
  backend:
    container_name: ai-breach-backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      OLLAMA_URL: http://192.168.1.8:11434